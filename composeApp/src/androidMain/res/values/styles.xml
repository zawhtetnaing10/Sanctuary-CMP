<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dialog Theme -->
    <style name="Theme.DialogFullScreen" parent="android:Theme.Material.Light.Dialog">
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.8</item>
    </style>

    <!-- App Parent Theme -->
    <style name="Theme.Sanctuary" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:dialogTheme">@style/Theme.DialogFullScreen</item>
    </style>
</resources>